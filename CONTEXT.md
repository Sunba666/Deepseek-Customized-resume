# CONTEXT — resume-advisor 领域术语表

> 本文件是项目的**领域术语表（glossary）**，只记录概念与术语，不包含实现细节。
> 术语一旦在此定稿，代码、文档、UI 文案必须统一使用。

## 核心概念

| 术语 | 定义 |
| --- | --- |
| 简历（Resume） | 用户上传的求职文档，支持 PDF / DOCX / TXT，仅做文本提取。 |
| 职业画像（Portrait） | 从简历解析结果推断出的求职者画像：目标岗位（含方向细分）、技能标签、经验年限、期望城市。用户可手动修正。 |
| 目标岗位（Target Role） | 用户确认的求职方向，如「数据分析师-偏业务方向」。可细化为行业 + 方向。 |
| 求职方案（Plan） | 一次完整的生成结果：职业画像 + 推荐公司列表（含风险标签/证据/投递渠道）+ 匹配度 + 简历优化建议。 |
| 推荐公司（Company） | 根据画像和城市推荐的 10~20 家目标公司。 |
| 风险核验（Risk Check） | 对推荐公司做的背景调查：聚合数据源的结构化风险指标 → 判定风险等级 → AI 摘要解释。 |
| 风险等级（Risk Level） | 🟢 正常 / 🟡 需注意 / 🔴 高风险。**不得由 AI 凭空判断**，必须基于数据源指标，数据不足标注「未知」。 |
| 风险证据（Evidence） | 每条风险结论必须附带的来源 URL（国家企业信用信息公示系统、裁判文书网、执行信息公开网、信用中国、官网备案等）。 |
| 投递渠道（Channel） | 官方投递途径：官网招聘页、认证招聘平台链接、HR 邮箱、内推码。 |
| 匹配度（Match Score） | 简历与目标岗位的匹配程度（技能 / 经验 / 学历 / 薪资，百分比）。 |
| STAR 优化（STAR Advice） | 基于 STAR 法则（S 情境 / T 任务 / A 行动 / R 结果）对简历经历类内容的改写建议。只作用于经历，不添加虚构经历。 |
| 脱敏（Redaction） | 对手机号、邮箱、身份证号、详细住址、照片等敏感信息做本地正则替换。 |
| 匿名简历（Anonymous Resume） | 脱敏后的简历导出，用于安全分享。 |
| 规则引擎（Rule Engine） | 无 LLM Key 时的本地兜底：正则 + 关键词 + 模板生成画像/摘要/建议，输出较生硬但可离线工作。 |

## 关键决策（ADR 索引）

- `docs/adr/0001-web-first-tauri-later.md` — 桌面壳策略：开发期 Web 优先，Tauri 最后封装
- `docs/adr/0002-local-company-db-with-mock-fallback.md` — 公司数据源：复用本地库 + mock 回退
- `docs/adr/0003-deepseek-default-rule-engine-fallback.md` — LLM 配置：DeepSeek 默认 + 规则引擎兜底
- `docs/adr/0004-markdown-print-pdf.md` — 导出：Markdown + 浏览器打印，避免系统依赖

## 数据流（用户视角）

上传简历 → 解析+脱敏 → 确认/修正岗位画像 → 生成方案（推荐公司 + 风险核验 + 匹配度 + 可选 STAR 建议）→ 查看结果 → 导出（Markdown/PDF，默认脱敏）。
