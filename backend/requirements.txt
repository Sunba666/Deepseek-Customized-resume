# === 运行环境 ===
# Python >= 3.11（开发机 3.14 已验证）

# === Web 框架 ===
fastapi>=0.115
uvicorn[standard]>=0.30

# === 简历解析 ===
pypdf>=5.0
python-docx>=1.1
python-pptx>=1.0

# === 配置与数据 ===
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# === LLM 调用 (OpenAI 兼容) ===
openai>=1.40

# === 本地公司库复用 (Company-lookup) ===
# sqlite3 为标准库，无需额外依赖

# === 测试 ===
pytest>=8.0
httpx>=0.27
python-multipart>=0.0.9
